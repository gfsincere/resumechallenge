apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resumechallenge.fullname" . }}-test-connection"
  labels:
    {{- include "resumechallenge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resumechallenge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
